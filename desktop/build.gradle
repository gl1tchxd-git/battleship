apply plugin: 'application'

dependencies {
    implementation project(':core')
    implementation "com.badlogicgames.gdx:gdx-backend-lwjgl3:$gdxVersion"
    implementation "com.badlogicgames.gdx:gdx-platform:$gdxVersion:natives-desktop"
    implementation "com.badlogicgames.gdx:gdx-freetype-platform:$gdxVersion:natives-desktop"
}

application {
    mainClass = 'at.gl1tchxd.battleship.DesktopLauncher'
}

jar {
    archiveBaseName = 'battleship-desktop'
    manifest {
        attributes 'Main-Class': 'at.gl1tchxd.battleship.DesktopLauncher'
    }
}

// Create a fat JAR with all dependencies
tasks.register('fatJar', Jar) {
    dependsOn configurations.runtimeClasspath
    archiveBaseName = 'battleship-desktop'
    archiveClassifier = 'all'
    manifest {
        attributes 'Main-Class': 'at.gl1tchxd.battleship.DesktopLauncher'
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
    with jar

    // Exclude signature files that can cause corruption issues
    exclude 'META-INF/*.SF'
    exclude 'META-INF/*.DSA'
    exclude 'META-INF/*.RSA'

    // Proper handling of duplicate files
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE

    // Merge service files properly
    filesMatching('META-INF/services/*') {
        duplicatesStrategy = DuplicatesStrategy.INCLUDE
    }

    // Preserve native library timestamps
    preserveFileTimestamps = false
    reproducibleFileOrder = true
}